on:
  push:
    paths:
      - .github/workflows/do-not-trigger-to-often.yml

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Hello world"
          echo "Running from ${{ github.workflow }}"
      
