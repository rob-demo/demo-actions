name: Use GitHub App
on:
  push:
    paths:
    - .github/workflows/use-github-app.yml

jobs:
  checkout-other-repo:
    runs-on: ubuntu
    steps:
    - uses: actions/checkout@v3
      with:
        token: ${{ github.token }}
        repository: rob-demo/example-demo
     
  
